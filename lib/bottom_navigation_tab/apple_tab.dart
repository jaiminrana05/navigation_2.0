import 'package:flutter/material.dart';
import 'package:simple_project/home_page.dart';

class AppleTab extends StatelessWidget {
  const AppleTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const HomePage(title: 'From First Page');
  }
}
